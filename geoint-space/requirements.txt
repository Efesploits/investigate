# The free Gradio tier runs on ZeroGPU, which only accepts torch 2.8–2.11 and
# schedules the GPU through the `spaces` package. gradio itself comes from the
# Space SDK (and brings fastapi/uvicorn); the model stack is what we add.
torch==2.8.0
spaces
transformers
pillow
