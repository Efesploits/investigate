node_modules/
.env

# local API key — never commit
desktop/secret.js

# stray runtime logs / build output
server.log
desktop/dist/

# local JSON-store fallback (dev only; prod uses Postgres via DATABASE_URL)
data/
