node_modules/
.env

# local API key — never commit
desktop/secret.js

# stray runtime logs / build output
server.log
desktop/dist/

# local JSON-store fallback (dev only; prod uses Firestore/Postgres)
data/

# firebase emulator debug logs
firebase-debug.log
firestore-debug.log
ui-debug.log
.firebase/
